# «Обход #2» в серверном автовыборе — 18.09.2026

Подтверждено добавление в действующий «⚡ Автовыбор Обхода»
(`sc.hambot.ru:4500`, Selectal `5.188.115.106`). Это серверный leastPing-пул,
а не изменение тегов клиентского списка и не «Автовыбор Серверов».

## Применено

- В выделенном профиле MWS добавлен отдельный VLESS/REALITY backend на 28443,
  собственные ключи и служебная идентичность. Клиентские порты 18443/2083,
  хост «Обход #2», его Firefox и self-steal не изменялись.
- Новый backend разрешает маршрутизацию только источнику Selectal
  `5.188.115.106`; остальные источники попадают в BLOCK. Это ограничение
  маршрутизации Xray, а не заявление о сетевом firewall ACL.
- Служебный сквад получает только новый backend; обычные клиентские сквады
  его не получили. Backend не опубликован отдельным клиентским хостом.
- В профиль Selectal добавлен один outbound `BS3_AUTO_MWS2`, Firefox.
  Существующие selectors уже включают префикс `BS3_AUTO_`; правила маршрутизации,
  observatory, стратегия, fallback и прежние outbounds сохранены побайтно в JSON.
- Путь: клиент → Selectal → MWS `176.109.85.244:28443` → существующий
  защищённый канал MWS → зарубежный выход `72.56.101.218`.
  Прямой новый Selectal→MWS канал работает; новый SSH-туннель не создавался.
  Российский DIRECT в автовыборе остался прежним, на Selectal.
- Правильный локальный SSH-ключ Selectal: `C:/Users/User/.ssh/ssh-key-trz`,
  комментарий `triazov@triazov-dev`. Существующая проверка ключа сервера сохранена.

## Подтверждения

- Оба кандидата проверены установленным Xray 26.3.27.
- Из namespace Selectal отдельный аутентифицированный канал: HTTP 204,
  egress `72.56.101.218`, Wikipedia HTTP 200, 119573 байта.
- Свежая реальная mihomo-подписка проверена отдельным Windows-процессом
  без изменения работающего HamVPN PC, TUN или системного прокси.
  В успешных повторных тестах автовыбор сам выбирал новый выход:
  204, `72.56.101.218`, Wikipedia целиком. Финальная проверка прошла.
- У новой служебной идентичности проверена изоляция сквада; обе ноды connected.
- Тестовый пользователь удалён, отсутствие подтверждено read-only SQL.
- Автоматический откат снят после успешных проверок. Отдельно подтверждены
  inactive timer и service, отсутствие rollback-маркера. После снятия повторены
  сверка профилей/прав и успешные backend 204 + egress + полная страница.
- Во время работы другой исполнитель поменял fingerprint хоста «Швеция»
  с Chrome на Firefox. Это независимое изменение сохранено, не откатывалось.

## Ограничения и наблюдения

Один промежуточный mihomo-прогон получил два таймаута, затем загрузил страницу;
следующие два полных прогона успешны. Это не гарантия отсутствия сетевых сбоев.

Из панели проверка фактического Happ/Xray Hysteria2 outbound не прошла
(curl 35) как **до**, так и после изменения. Причина в этой операции не доказана;
не считать это исправлением всех Hysteria2-клиентов или всех операторских сетей.
Работоспособность нового межсерверного VLESS и настоящего клиента mihomo
подтверждена отдельно. LeastPing не обязан постоянно выбирать именно MWS.

## Снимок и восстановление

Закрытое состояние на панели: `/root/ham-mws-auto-20260918`, root-only.
Канонический JSON SHA-256 снимка `before`:
`0ecf6a9c9379968ea5e79baafd5dc4c7b7d568b137c94558a62dcf80b89b3f63`.
Ключи, служебные UUID, полные подписки и снимки в Git не публикуются.

Скрипт отката предназначен для текущего окна и проверяет drift. После завершения
не запускать его вслепую: тестовый аккаунт уже удалён, исходные профили могли
измениться. Будущее удаление маршрута требует свежего снимка и новой проверки
владения. Приоритет — сначала убрать только outbound из пула, затем удалять
неиспользуемые backend/служебные права. Не восстанавливать целиком старую БД.

Код применялся из опубликованных GitHub-коммитов: 409cc95dcbeb (stage/apply),
e7767a35d38d и 8143c32d032a (клиентская проверка и завершение).
