# UK bypass recurrence — 2026-09-10

## Status

The reported outage is reproduced on the public route. It is **not repaired**.
The UK firewall defect fixed on September 9 has not recurred. Access to the
separately administered Russian ingress is required to diagnose and repair
the remaining path failure. No production configuration was changed today.

## Observations (13:24–13:40 UTC)

Route: `client → 188.225.62.121:9443 → 51.194.229.165:7443 → Internet`.

- The UK node remains connected to the panel, with the same enabled host,
  inbound binding, profile, and REALITY key fingerprint in the panel and
  node startup configuration. No key material is recorded here.
- UFW still allows TCP/7443 from the existing Russian bridge only. Its
  counter is increasing; the listener is present. Container restart count
  remains zero, with its original August 27 start time.
- Local TLS to the UK listener succeeds with TLS 1.3 and verified
  `google.com` certificate. Direct Internet requests return HTTP 204 from
  Google and HTTP 200 from Cloudflare. CPU, memory, and conntrack usage
  show no resource exhaustion.
- An authenticated VLESS/REALITY **Vision** request through the existing
  UK inbound on loopback returns HTTP 204. This exercises the actual
  inbound, authentication, and outbound, not only REALITY's TLS fallback.
  The probe uses an existing active entitlement in memory; it creates no
  account and changes no entitlement. Temporary client processes are stopped.
- Initial probe attempts without Vision failed. These were incorrectly
  constructed probes, not proof of a production flow defect. The panel's
  deployed flow helper infers Vision for raw/TCP + REALITY when no explicit
  flow override is set. Startup configuration alone is insufficient to
  infer dynamically installed user flow.
- TCP connections to the Russian ingress time out from the Russian control
  server, the panel server, and the UK node. Tests included 9443, 443, and
  22; a further control-server test also covered 7443.
- The local workstation can obtain an SSH banner from the Russian ingress,
  but TLS to 9443 fails. A completed local socket connect is not treated as
  proof that a proxied/VPN network delivered the subsequent handshake.
- Authenticated Vision probes routed through the local workstation and
  the public Russian endpoint fail with both Firefox and Chrome fingerprints.
  Across the two bounded attempts the local relay sent 3,605 bytes and
  received zero bytes. Its temporary SSH reverse listener was removed.
- A narrow UK packet capture also sees some bridge connections completing
  TCP handshakes and exchanging payload in both directions. The failure is
  therefore selective/intermittent, not a proven universal TCP block.
- Before synthetic probes, the latest per-user traffic update found was
  13:14:00.718 UTC, with no updates in the preceding ten-minute check.
  Later synthetic traffic must not be counted as recovered customer traffic.

## Required next access/checks

The Russian ingress administrator must correlate a failing client attempt
with packet captures on its public TCP/9443 and UK-facing TCP/7443 paths.
Check the 9443 listener or NAT mapping, firewall/anti-DDoS filtering, routing,
and return traffic. Source-dependent reachability is observed, but the
responsible device or filtering policy is not established without that access.

Ordinary TLS still uses TCP and is not a demonstrated remedy for this failure.
Do not replace the working UK REALITY listener, open its port globally,
restart the node, or switch the public host to the unrelated Russian 7443
service based only on the current observations. No mobile-operator-wide
availability claim is supported by these tests.
